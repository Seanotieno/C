#include<stdio.h>
#include<maths.h>
int main(){
    int initial,final,cube;
    printf("enter the initial and final value");
    scanf("%d%d",&initial,&final);

    if(start<=final{
        for(initial;initial<=final;initial++){
            cube=pow(initial,3);
            printf("the cube of %d is :%d\n",initial,cube);
        }
    else{
        printf("error");
    }
    return 0;
}